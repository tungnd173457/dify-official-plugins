identity:
  name: get_document_with_metadata_filtering
  author: tungnd
  label:
    en_US: Get document with metadata filtering

description:
  human:
    en_US: Get document with metadata filtering. filter_conditions must be valid JSON string
  llm: Get document with metadata filtering. filter_conditions must be valid JSON string

parameters:
  - name: dataset_id
    type: string
    label:
      en_US: Dataset ID
    human_description:
      en_US: >-
        Dataset ID
    required: true
    default: ""
    form: llm

  - name: filter_conditions
    type: string
    label:
      en_US: Filter Conditions
    human_description:
      en_US: |
        Must be a valid JSON string
    required: true
    default: ""
    form: llm

extra:
  python:
    source: tools/get_document_with_metadata_filtering/get_document_with_metadata_filtering.py
